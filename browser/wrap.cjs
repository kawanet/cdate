"use strict"; // PRE
var cdate, strftime; // PRE
!((__then) => { // PRE

    __then(X_cdate, X_strftime);
})((X_cdate, X_strftime) => {
    const __exports = ("undefined" !== typeof exports ? exports : {});
    cdate = __exports.cdate = X_cdate;
    strftime = __exports.strftime = X_strftime;
});
